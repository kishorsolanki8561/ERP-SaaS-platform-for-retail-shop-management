# The real Dockerfile lives at src/ErpSaas.Api/Dockerfile
# (Visual Studio requires the Dockerfile inside the project folder.)
#
# docker-compose.yml references it as:
#   build:
#     context: ../src
#     dockerfile: ErpSaas.Api/Dockerfile
